const size = 3;

for (let row = 0; row < size; row++) {
  const lineChars = Array(size)
    .fill()
    .map((_, col) => ((row + col) % 2 === 0 ? "x" : "o"));
    
  console.log(lineChars.join(" "));
}
